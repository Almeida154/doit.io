import { createTheme } from 'styles';

export const darkTheme = createTheme('dark-theme', {
  colors: {
    title: '#FFFFFF',
    text: '#666666',
    background: '#000000',
    container: '#1A1A1A',
  },
});
