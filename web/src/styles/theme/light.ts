import { createTheme } from 'styles';

export const lightTheme = createTheme('light-theme', {
  colors: {
    title: '#000000',
    text: '#1A1A1A',
    background: '#FFFFFF',
    container: '#F6F6F6',
  },
});
